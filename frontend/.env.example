# SatQuery AI - Frontend Environment Configuration
# API keys must remain server-side only in backend/.env

# Deployed Render Backend API URL (leave blank or comment out to use local Vite proxy)
VITE_API_URL=https://satquery-le25.onrender.com
